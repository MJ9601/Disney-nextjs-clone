import React, { ReactElement } from "react";

const HeaderOption = ({
  Icon,
  title,
}: {
  Icon: ReactElement;
  title: string;
}) => {
  return <div>HeaderOption</div>;
};

export default HeaderOption;
